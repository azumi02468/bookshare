<div class="modal" role="dialog" area-hidden="true" id="message<%= i %>">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">メッセージを送る</h4>
            </div>
            <div class="modal-body">
                <p><%= data[i].owner_name %>さんへのメッセージをお書きください。</p>
                <form name="message" method="post" action="/search/message" novalidate>
                    <div class="form-group">
                        <label for="target_book">対象書籍名</label>
                        <input type="text" class="form-control" name="target_book" id="target_book" value="<%= data[i].title %>" readonly />
                    </div>
                    <div class="form-group">
                        <label for="ms_body">メッセージ内容</label>
                        <textarea class="form-control" name="ms_body" id="ms_body" ng-model="ms_body" maxlength="100" required ></textarea>
                        <p ng-show="message.ms_body.$error.required">入力必須です。</p>
                    </div>
                    <input type="hidden" name="to_user" id="to_user" value="<%= data[i].owner_id %>" />
                    <div class="form-group">
                        <button class="btn btn-success" ng-disabled="message.$invalid">送信</button>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button class="btn btn-primary" data-dismiss="modal">閉じる</button>
            </div>
        </div>
    </div>
</div>